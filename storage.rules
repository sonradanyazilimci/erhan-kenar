rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Yüklenen görseller herkese açık görüntülenir; sadece giriş yapmış admin yükleyebilir/silebilir.
    match /uploads/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
